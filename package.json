{
  "name": "test_cypress",
  "version": "0.0.0",
  "description": "Cypress tests for tags filtering",
  "scripts": {
    "test-tag": "DEBUG=@cypress/grep npx cypress run --env grepTags=config"
  },
  "author": "Me",
  "license": "UNLICENSED",
  "devDependencies": {
    "@cypress/grep": "^4.0.1",
    "cypress": "^13.7.3",
    "typescript": "^4.6.3"
  }
}
